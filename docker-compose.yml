services:
  db:
    image: postgres:15
    container_name: pfa-postgres
    environment:
      POSTGRES_USER: pfa_user
      POSTGRES_PASSWORD: pfa_pass
      POSTGRES_DB: pfa_db
    ports:
      - "55432:5432"
    volumes:
      - pfa-postgres-data:/var/lib/postgresql/data
volumes:
  pfa-postgres-data:
